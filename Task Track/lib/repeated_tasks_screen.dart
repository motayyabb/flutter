import 'package:flutter/material.dart';

class RepeatedTasksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Repeated Tasks will be displayed here.'));
  }
}
