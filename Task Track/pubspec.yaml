name: tasktrack
description: A task management app using Flutter and SQLite.

# The version of the app.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path_provider: ^2.0.11
  # Add `flutter/foundation.dart` to check the platform type
  foundation: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
